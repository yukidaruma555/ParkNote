<!DOCTYPE html>
<html>
  <head>
    <title>Parknote</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/e71241d1d0.js" crossorigin="anonymous"></script>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
   <header>
     <nav class="navber navbar-expand-lg navber-dark bg-green">
       <div class="container">
     <% if user_signed_in? %>
       <li>
         <%= link_to "公園登録", new_park_path %>
       </li>
       <li>
         <%= link_to "公園一覧", parks_path %>
       </li>
       <li>
       <%= link_to "投稿一覧", posts_path %>
     </li>
     <li>
     <%= link_to "マイページ", user_path(current_user.id) %>
   </li>
       <li>
         <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
      </li>
    <% else %>
      <li>
        <%= link_to "新規登録", new_user_registration_path %>
      </li>
      <li>
        <%= link_to "ログイン", new_user_session_path %>
      </li>
      <li>
        <%= link_to "アバウト", about_path %>
      </li>
    <% end %>
      </div>
    </nav>
  </header>
    <%= yield %>
  </body>
</html>
